apiVersion: v1
kind: Service
metadata:
  labels:
    component: chat
    app: workadventure
  name: chat
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    component: chat
    app: workadventure
  sessionAffinity: None
  type: ClusterIP